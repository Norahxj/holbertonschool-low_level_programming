#include <stdio.h>

/**
 *
 * main - digits from 0 to 10
 *
 * Description: print all single digits numbers
 *
 * Return: 0 after success
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(n + '0');
	putchar('\n')

		return (0);
}
